---
title: Stack
---

import { Editor } from '..'
import Stack from '../examples/stack'

# Stack

This layout uses CSS Grid to add space between child elements,
without needing to revert to unfamiliar CSS selector syntax.

<Editor component={Stack} />

