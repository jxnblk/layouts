---
title: Tiles by Columns
---

import { Editor } from '..'
import Tiles from '../examples/tiles-by-columns'

# Tiles by Columns

This layout is similar to the [Tiles by Width](/tiles-by-width) example, but creates a specific number of columns.
Using the array-based syntax in Rebass or Theme UI makes this easy to change across breakpoints.

<Editor component={Tiles} />

