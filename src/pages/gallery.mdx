---
title: Gallery (Grid)
---

import { Editor } from '..'
import Gallery from '../examples/gallery'

# Gallery (Grid)

Layout creates a gallery type view using `grid-template-areas` to handle boxes position.

<Editor component={Gallery} />
