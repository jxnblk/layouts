---
title: Tiles by Width
---

import { Editor } from '..'
import Tiles from '../examples/tiles-by-width'

# Tiles by Width

This layout uses CSS Grid to create evenly sized tiles that are at least as wide as the `256px` value provided.
The width value can be passed into the component as a prop to make this more reusable.

<Editor component={Tiles} />

