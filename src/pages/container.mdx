---
title: Container
---

import { Editor } from '..'
import Container from '../examples/container'

# Container

This layout creates a centered max-width box.
The max-width and default padding can optionally be set in the theme object for reuse.

<Editor component={Container} />

